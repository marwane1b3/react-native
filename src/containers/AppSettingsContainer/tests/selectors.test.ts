// import { selectAppSettingsContainerDomain } from '../selectors';

describe('selectAppSettingsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
