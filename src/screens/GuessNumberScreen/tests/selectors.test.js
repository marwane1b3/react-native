// import { selectGuessNumberScreenDomain } from '../selectors';

describe('selectGuessNumberScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
