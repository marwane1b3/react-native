// import { selectMapViewScreenDomain } from '../selectors';

describe('selectMapViewScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
